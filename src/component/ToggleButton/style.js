import {StyleSheet} from 'react-native';

export default {
    toggleButtonContainer: {
        borderRadius: 5,
        padding: 15,
        justifyContent: 'center',
        alignItems: 'center',
    }
}